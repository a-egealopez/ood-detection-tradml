[tool.poetry]
name = "anomaly-detection-health-finance"
version = "0.1.0"
description = "Unsupervised anomaly detection: IoT health + stock volatility"
authors = ["Tu Nombre <tu-email@example.com>"]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.24"
pandas = "^2.0"
scikit-learn = "^1.3"
scipy = "^1.11"
streamlit = "^1.28"
plotly = "^5.17"
yfinance = "^0.2.28"
python-dotenv = "^1.0"
hmmlearn = "^0.2.7"
tick = "^0.7.1"
pyod = "^1.1.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
